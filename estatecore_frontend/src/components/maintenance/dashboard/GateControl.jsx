import {useState} from "react";import axios from "axios";export default function GateControl({propertyId,gateList}){const[status,setStatus]=useState("");const unlock=async(id)=>{try{const res=await axios.post("/api/unlock-gate",{gate_id:id,property_id:propertyId});setStatus(res.data.message);}catch(e){setStatus("Error");}};return <div>{gateList.map(g=><button key={g.id} onClick={()=>unlock(g.id)}>Unlock {g.name}</button>)}<p>{status}</p></div>;}
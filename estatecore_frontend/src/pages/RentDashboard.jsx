import {useEffect,useState} from 'react';import {api} from '../utils/api';export default function RentDashboard(){const [data,setData]=useState(null);const [month,setMonth]=useState(new Date().toISOString().slice(0,7));useEffect(()=>{api('GET',`/rent/metrics?month=${month}`).then(setData).catch(console.error)},[month]);return (<div><h2>Rent Dashboard</h2><label>Month: <input type='month' value={month} onChange={e=>setMonth(e.target.value)}/></label>{data&&(<ul><li>Total: {data.total_rent}</li><li>Collected: {data.collected}</li><li>Net: {data.net}</li></ul>)}</div>)}

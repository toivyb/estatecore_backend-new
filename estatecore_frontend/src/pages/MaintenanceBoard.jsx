import {useEffect,useState} from 'react';import {api} from '../utils/api';const COLS=['open','in_progress','needs_info','on_hold','completed','canceled'];export default function MaintenanceBoard(){const [items,setItems]=useState([]);useEffect(()=>{api('GET','/workorders').then(setItems)},[]);return (<div style={{display:'grid',gridTemplateColumns:'repeat(6,1fr)',gap:12}}>{COLS.map(col=>(<div key={col} style={{border:'1px solid #ddd',padding:8}}><h3>{col}</h3>{items.filter(i=>i.status===col).map(i=>(<div key={i.id} style={{border:'1px solid #ccc',marginBottom:8,padding:6}}><div><b>#{i.id}</b> {i.title}</div><div>Priority: {i.priority}</div><div>Assignee: {i.assignee||'-'}</div></div>))}</div>))}</div>)}

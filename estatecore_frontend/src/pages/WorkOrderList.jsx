import {useEffect,useState} from 'react';import {api} from '../utils/api';export default function WorkOrderList(){const [items,setItems]=useState([]);const [status,setStatus]=useState('');const [priority,setPriority]=useState('');async function load(){const qs=new URLSearchParams();if(status)qs.set('status',status);if(priority)qs.set('priority',priority);setItems(await api('GET',`/workorders?${qs.toString()}`))}useEffect(()=>{load()},[status,priority]);return (<div><h2>Work Orders</h2><div><label>Status <select value={status} onChange={e=>setStatus(e.target.value)}><option value=''>all</option><option>open</option><option>in_progress</option><option>needs_info</option><option>on_hold</option><option>completed</option><option>canceled</option></select></label><label> Priority <select value={priority} onChange={e=>setPriority(e.target.value)}><option value=''>all</option><option>low</option><option>normal</option><option>high</option><option>urgent</option></select></label></div><table border='1' cellPadding='6' style={{marginTop:12,width:'100%'}}><thead><tr><th>ID</th><th>Title</th><th>Status</th><th>Priority</th><th>Assignee</th></tr></thead><tbody>{items.map(x=>(<tr key={x.id}><td>{x.id}</td><td>{x.title}</td><td>{x.status}</td><td>{x.priority}</td><td>{x.assignee||'-'}</td></tr>))}</tbody></table></div>)}

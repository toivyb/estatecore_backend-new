import {useEffect,useState} from 'react';import {api} from '../utils/api';export default function Reports(){const [month,setMonth]=useState(new Date().toISOString().slice(0,7));const [rent,setRent]=useState(null);const [wo,setWo]=useState(null);useEffect(()=>{api('GET',`/reporting/rent-summary?month=${month}`).then(setRent);api('GET','/reporting/workorder-status').then(setWo)},[month]);return (<div><h2>Reports</h2><label>Month: <input type='month' value={month} onChange={e=>setMonth(e.target.value)}/></label><div style={{marginTop:12}}><h3>Rent</h3><pre>{JSON.stringify(rent,null,2)}</pre><h3>Work Orders</h3><pre>{JSON.stringify(wo,null,2)}</pre></div></div>)}

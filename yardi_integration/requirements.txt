# Yardi Integration Requirements
# Additional dependencies for the comprehensive Yardi property management integration

# Core dependencies (ensure compatibility with existing EstateCore requirements)
aiohttp>=3.8.0
asyncio-mqtt>=0.11.0
croniter>=1.3.0
cryptography>=38.0.0
pyjwt>=2.6.0
schedule>=1.2.0

# Optional dependencies for enhanced features
xmltodict>=0.13.0  # For XML parsing in legacy Yardi systems
python-dateutil>=2.8.2  # Enhanced date parsing
pytz>=2023.3  # Timezone handling
redis>=4.5.0  # For distributed caching (optional)
celery>=5.2.0  # For background task processing (optional)

# Development and testing dependencies
pytest>=7.2.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
responses>=0.22.0  # For mocking HTTP requests in tests

# Documentation dependencies (optional)
sphinx>=5.3.0
sphinx-rtd-theme>=1.2.0